function tsfc = p1_get_tsfc(M , theta)

tsfc = (0.4 + (0.45*M))*sqrt(theta);

%change "output" and "inputs" to your variables
% p1_get_tsfc
% Computes TSFC (treated as 1/hr).

% Inputs (order matters):
%   (1) M
%   (2) theta

% Output:
%   tsfc

% TODO: implement the provided TSFC equation

end
